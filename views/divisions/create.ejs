<!-- Path: /views/divisions/create.ejs -->
<%- include('../partials/header', { 
    title: 'Create Division',
    includeChartJS: false
}) %>
<%- include('../partials/navigation', { 
    activeMenu: 'divisions',
    user: typeof user !== 'undefined' ? user : null
}) %>

<!-- Main Content -->
<main class="flex-1 py-8">
    <div class="mx-auto max-w-full px-4 sm:px-6 lg:px-8">
        <div class="max-w-3xl mx-auto">
            <div class="bg-gradient-to-br from-white to-gray-50 rounded-2xl shadow-2xl border border-gray-100">
                <div class="px-6 py-4 bg-gradient-to-r from-orange-600 to-red-700 rounded-t-2xl">
                    <div class="flex items-center justify-between">
                        <h2 class="text-xl font-bold text-white flex items-center">
                            <span class="text-2xl mr-3"><i class="fas fa-sitemap"></i></span>
                            Create New Division
                        </h2>
                        <a href="/divisions" class="text-white/80 hover:text-white transition-colors p-2 rounded-full hover:bg-white/10">
                            <i class="fas fa-times text-lg"></i>
                        </a>
                    </div>
                </div>
                
                <form method="post" action="/divisions" class="form-section">
                    <% if (typeof csrfToken !== 'undefined') { %>
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <% } %>
                
                <div class="space-y-4">
                    <!-- Company Selection -->
                    <div class="form-group-enhanced">
                        <label for="company_code" class="block text-sm font-medium text-gray-800 font-semibold flex items-center">
                            <span class="text-lg mr-2">🏢</span>
                            Company <span class="text-red-500">*</span>
                        </label>
                        <select id="company_code" 
                                name="company_code" 
                                required
                                class="form-input-enhanced">
                            <option value="">Select a company</option>
                            <% if (typeof companies !== 'undefined' && companies) { %>
                                <% companies.forEach(function(company) { %>
                                    <option value="<%= company.company_code %>">
                                        <%= company.company_code %> - <%= company.company_name_th %>
                                    </option>
                                <% }); %>
                            <% } %>
                        </select>
                    </div>
                    
                    <!-- Branch Selection -->
                    <div class="form-group-enhanced">
                        <label for="branch_code" class="block text-sm font-medium text-gray-800 font-semibold flex items-center">
                            <span class="text-lg mr-2"><i class="fas fa-sitemap"></i></span>
                            Branch
                        </label>
                        <select id="branch_code" 
                                name="branch_code"
                                class="form-input-enhanced">
                            <option value="">Company Level (No Branch)</option>
                        </select>
                        <p class="mt-1 text-sm text-gray-500">
                            Leave empty if this division is at the company level
                        </p>
                    </div>
                    
                    <!-- Division Code -->
                    <div class="form-group-enhanced">
                        <label for="division_code" class="block text-sm font-medium text-gray-800 font-semibold flex items-center">
                            <span class="text-lg mr-2">🏷️</span>
                            Division Code <span class="text-red-500">*</span>
                        </label>
                        <input type="text" 
                               id="division_code" 
                               name="division_code" 
                               required
                               maxlength="20"
                               pattern="[A-Z0-9]+"
                               class="form-input-enhanced uppercase"
                               placeholder="e.g., DIV001">
                        <p class="mt-1 text-sm text-gray-500">
                            Use uppercase letters and numbers only. This cannot be changed later.
                        </p>
                    </div>
                    
                    <!-- Division Name -->
                    <div class="form-group-enhanced">
                        <label for="division_name" class="block text-sm font-medium text-gray-800 font-semibold flex items-center">
                            <span class="text-lg mr-2">📁</span>
                            Division Name <span class="text-red-500">*</span>
                        </label>
                        <input type="text" 
                               id="division_name" 
                               name="division_name" 
                               required
                               maxlength="200"
                               class="form-input-enhanced"
                               placeholder="e.g., Human Resources">
                    </div>
                    
                    <!-- Status -->
                    <div class="form-group-enhanced">
                        <label class="block text-sm font-medium text-gray-800 font-semibold flex items-center">
                            <span class="text-lg mr-2">⚡</span>
                            Status
                        </label>
                        <div class="flex items-center space-x-6 mt-2">
                            <label class="inline-flex items-center">
                                <input type="radio" 
                                       name="is_active" 
                                       value="true" 
                                       checked
                                       class="text-primary-600 ">
                                <span class="ml-2 text-sm text-gray-700">Active</span>
                            </label>
                            <label class="inline-flex items-center">
                                <input type="radio" 
                                       name="is_active" 
                                       value="false"
                                       class="text-primary-600 ">
                                <span class="ml-2 text-sm text-gray-700">Inactive</span>
                            </label>
                        </div>
                    </div>
                </div>
                
                <!-- Form Actions -->
                <div class="mt-8 pt-6 border-t border-gray-200 flex items-center justify-between">
                    <a href="/divisions" class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 hover:bg-gray-100 transition-all duration-200">
                        <i class="fas fa-arrow-left mr-2"></i>
                        Cancel
                    </a>
                    
                    <div class="flex items-center space-x-3">
                        <button type="reset" class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 border-gray-300 text-gray-600 hover:bg-gray-50 hover:border-gray-400 transition-all duration-200">
                            <i class="fas fa-redo mr-2"></i>
                            Reset
                        </button>
                        <button type="submit" id="submit-btn" class="btn btn-primary">
                            <span class="btn-text">
                                <span style="font-size: 1rem; margin-right: 0.5rem;">💾</span>
                                บันทึกฝ่าย
                            </span>
                            <span class="btn-loading hidden">
                                <span class="spinner mr-2"></span>
                                กำลังบันทึก...
                            </span>
                        </button>
                    </div>
                </div>
            </form>
        </div>
        
        <!-- Help Section -->
        <div class="mt-6 bg-gradient-to-r from-orange-50 to-red-50 rounded-2xl p-6 border border-orange-100 shadow-lg">
            <div class="flex">
                <div class="flex-shrink-0">
                    <span class="text-2xl"><i class="fas fa-lightbulb"></i></span>
                </div>
                <div class="ml-4">
                    <h3 class="text-lg font-bold text-orange-800 flex items-center">
                        <span class="mr-2"><i class="fas fa-book"></i></span>
                        Guidelines
                    </h3>
                    <div class="mt-3 text-sm text-orange-700">
                        <ul class="space-y-2">
                            <li class="flex items-start">
                                <span class="text-orange-500 mr-2 mt-0.5">•</span>
                                Division code cannot be changed after creation
                            </li>
                            <li class="flex items-start">
                                <span class="text-orange-500 mr-2 mt-0.5">•</span>
                                Division can be at company level or under a specific branch
                            </li>
                            <li class="flex items-start">
                                <span class="text-orange-500 mr-2 mt-0.5">•</span>
                                Division code must be unique across the entire system
                            </li>
                            <li class="flex items-start">
                                <span class="text-orange-500 mr-2 mt-0.5">•</span>
                                All divisions start as active by default
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>

<%- include('../partials/footer', {
    pageScripts: `
    <script>
    // Auto uppercase for division code
    document.getElementById('division_code').addEventListener('input', function(e) {
        e.target.value = e.target.value.toUpperCase();
    });
    
    // Load branches when company is selected
    document.getElementById('company_code').addEventListener('change', function() {
        const companyCode = this.value;
        const branchSelect = document.getElementById('branch_code');
        
        // Clear branch options
        branchSelect.innerHTML = '<option value="">Company Level (No Branch)</option>';
        
        if (companyCode) {
            // Fetch branches for selected company
            fetch(\`/api/companies/\${companyCode}/branches\`)
                .then(response => response.json())
                .then(data => {
                    if (data.success && data.data) {
                        data.data.forEach(branch => {
                            const option = document.createElement('option');
                            option.value = branch.branch_code;
                            option.textContent = \`\${branch.branch_code} - \${branch.branch_name}\`;
                            if (branch.is_headquarters) {
                                option.textContent += ' (Headquarters)';
                            }
                            branchSelect.appendChild(option);
                        });
                    }
                })
                .catch(error => {
                    console.error('Error loading branches:', error);
                });
        }
    });
    
    // Form submission loading state
    document.querySelector('form').addEventListener('submit', function() {
        const submitBtn = document.getElementById('submit-btn');
        const btnText = submitBtn.querySelector('.btn-text');
        const btnLoading = submitBtn.querySelector('.btn-loading');
        
        submitBtn.disabled = true;
        btnText.classList.add('hidden');
        btnLoading.classList.remove('hidden');
    });
    </script>
    `
}) %>