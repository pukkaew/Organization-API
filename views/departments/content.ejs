<!-- Path: /views/departments/content.ejs -->
    <div class="space-y-6">
    <!-- Page Header -->
    <div class="bg-white rounded-lg shadow-md border border-gray-200 fast-card mb-6">
        <div class="p-6">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-4">
                    <div class="w-16 h-16 bg-gradient-to-br from-emerald-500 via-emerald-600 to-emerald-700 rounded-2xl flex items-center justify-center shadow-xl hover:shadow-2xl transition-all duration-300 transform hover:scale-105 hover:rotate-2 border border-emerald-400/20">
                        <span class="text-white text-3xl drop-shadow-xl filter brightness-110"><i class="fas fa-users"></i></span>
                    </div>
                    <div>
                        <h1 class="text-2xl font-bold text-gray-900">
                            Departments
                        </h1>
                        <p class="text-gray-600">Manage departments within your divisions</p>
                    </div>
                </div>
                <div>
                    <a href="/departments/new" class="btn btn-success">
                        <span style="font-size: 1.125rem; margin-right: 0.5rem;">➕</span>
                        เพิ่มแผนกใหม่
                    </a>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Filters Section -->
    <div class="card-elevated fast-card mb-6">
        <div class="p-6">
            <div class="flex items-center space-x-3 mb-4">
                <span class="text-xl">🔍</span>
                <h2 class="text-lg font-semibold text-gray-900">
                    Search & Filter
                </h2>
            </div>
            <form method="get" action="/departments" class="fast-form grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
                <div>
                    <label for="search" class="block text-sm font-medium text-gray-700 mb-1">Search</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <i class="fas fa-search text-gray-400"></i>
                        </div>
                        <input type="text" 
                               name="search" 
                               id="search" 
                               value="<%= typeof query !== 'undefined' && query.search || '' %>"
                               placeholder="Search by code or name..."
                               class="form-input-enhanced w-full pl-10">
                    </div>
                </div>
                
                <div>
                    <label for="company_code" class="block text-sm font-medium text-gray-700 mb-1">Company</label>
                    <select name="company_code" id="company_code" class="form-input-enhanced w-full">
                        <option value="">All Companies</option>
                        <% if (typeof companies !== 'undefined' && companies.length > 0) { %>
                            <% companies.forEach(function(company) { %>
                                <option value="<%= company.company_code %>" <%= typeof query !== 'undefined' && query.company_code === company.company_code ? 'selected' : '' %>>
                                    <%= company.company_code %> - <%= company.company_name_th || company.company_name_en || '' %>
                                </option>
                            <% }); %>
                        <% } %>
                    </select>
                </div>
                
                <div>
                    <label for="division_code" class="block text-sm font-medium text-gray-700 mb-1">Division</label>
                    <select name="division_code" id="division_code" class="form-input-enhanced w-full">
                        <option value="">All Divisions</option>
                        <% if (typeof divisions !== 'undefined' && divisions.length > 0) { %>
                            <% divisions.forEach(function(division) { %>
                                <option value="<%= division.division_code %>" <%= typeof query !== 'undefined' && query.division_code === division.division_code ? 'selected' : '' %>>
                                    <%= division.division_code %> - <%= division.division_name %>
                                </option>
                            <% }); %>
                        <% } %>
                    </select>
                </div>
                
                <div>
                    <label for="is_active" class="block text-sm font-medium text-gray-700 mb-1">Status</label>
                    <select name="is_active" id="is_active" class="form-input-enhanced w-full">
                        <option value="">All Status</option>
                        <option value="true" <%= typeof query !== 'undefined' && query.is_active === 'true' ? 'selected' : '' %>>Active</option>
                        <option value="false" <%= typeof query !== 'undefined' && query.is_active === 'false' ? 'selected' : '' %>>Inactive</option>
                    </select>
                </div>
                
                <div class="sm:col-span-2 lg:col-span-4 flex items-end">
                    <button type="submit" class="btn btn-primary no-gradient w-full">
                        <span style="font-size: 0.875rem; margin-right: 0.5rem;">🔍</span>
                        Apply Filters
                    </button>
                </div>
            </form>
        </div>
    </div>
    
    <!-- Departments Table -->
    <div class="overflow-hidden shadow ring-1 ring-black ring-opacity-5 md:rounded-lg fast-table">
        <div class="overflow-x-auto">
            <div class="table-wrapper">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-green-600 text-white">
                        <tr>
                            <th scope="col" class="px-6 py-4 text-left text-xs font-bold text-white uppercase tracking-wider">
                                # Department Code
                            </th>
                            <th scope="col" class="px-6 py-4 text-left text-xs font-bold text-white uppercase tracking-wider">
                                <i class="fas fa-users mr-2"></i>Department Name
                            </th>
                            <th scope="col" class="px-6 py-4 text-left text-xs font-bold text-white uppercase tracking-wider">
                                <i class="fas fa-map mr-2"></i>Division
                            </th>
                            <th scope="col" class="px-6 py-4 text-left text-xs font-bold text-white uppercase tracking-wider">
                                <i class="fas fa-building"></i> <span class="hidden lg:inline">Company/</span>Branch
                            </th>
                            <th scope="col" class="px-6 py-4 text-center text-xs font-bold text-white uppercase tracking-wider">
                                <i class="fas fa-chart-bar mr-2"></i>Status
                            </th>
                            <th scope="col" class="px-6 py-4 text-center text-xs font-bold text-white uppercase tracking-wider">
                                <i class="fas fa-cogs mr-2"></i>Actions
                            </th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        <% if (typeof departments !== 'undefined' && departments.length > 0) { %>
                            <% departments.forEach(function(department, index) { %>
                                <tr class="<%= index % 2 === 0 ? 'bg-white' : 'bg-gray-50' %> hover:bg-blue-50 hover:shadow-md transition-all duration-200">
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="flex items-center">
                                            <div class="w-10 h-10 bg-gradient-to-br from-emerald-500 to-emerald-700 rounded-xl flex items-center justify-center mr-3 shadow-md hover:shadow-lg transition-all duration-200">
                                                <span class="text-white text-base drop-shadow-sm"><i class="fas fa-users"></i></span>
                                            </div>
                                            <div class="text-sm font-medium text-gray-900">
                                                <%= department.department_code %>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="text-sm text-gray-900">
                                            <%= department.department_name %>
                                        </div>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="text-sm text-gray-500">
                                            <%= department.division_code %> - <%= department.division_name %>
                                        </div>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="text-sm text-gray-500">
                                            <div><%= department.company_name %></div>
                                            <% if (department.branch_name) { %>
                                                <div class="text-xs text-gray-400">
                                                    <%= department.branch_name %>
                                                </div>
                                            <% } %>
                                        </div>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-center">
                                        <% if (department.is_active) { %>
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold bg-green-100 text-green-800">
                                                <i class="fas fa-check-circle"></i>
                                                Active
                                            </span>
                                        <% } else { %>
                                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold bg-gray-100 text-gray-800">
                                                <i class="fas fa-times-circle"></i>
                                                Inactive
                                            </span>
                                        <% } %>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-center">
                                        <div class="flex justify-center space-x-1 sm:space-x-2">
                                            <a href="/departments/<%= department.department_code %>" 
                                               class="inline-flex items-center px-2 sm:px-3 py-2 bg-blue-500 text-white rounded-md hover:bg-blue-600 transition-all duration-200 shadow-sm"
                                               title="View Details">
                                                <span class="text-sm sm:text-base"><i class="fas fa-eye"></i></span>
                                            </a>
                                            <a href="/departments/<%= department.department_code %>/edit" 
                                               class="inline-flex items-center px-2 sm:px-3 py-2 bg-yellow-500 text-white rounded-md hover:bg-yellow-600 transition-all duration-200 shadow-sm"
                                               title="Edit">
                                                <span class="text-sm sm:text-base"><i class="fas fa-edit"></i></span>
                                            </a>
                                            <form action="/departments/<%= department.department_code %>/toggle-status" 
                                                  method="POST" 
                                                  class="inline">
                                                <% if (typeof csrfToken !== 'undefined') { %>
                                                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                                <% } %>
                                                <button type="submit" 
                                                        class="inline-flex items-center px-2 sm:px-3 py-2 bg-<%= department.is_active ? 'orange-500 hover:bg-orange-600' : 'green-500 hover:bg-green-600' %> text-white rounded-md transition-all duration-200 shadow-sm" 
                                                        title="<%= department.is_active ? 'Deactivate' : 'Activate' %>"
                                                        onclick="return confirm('Are you sure you want to <%= department.is_active ? 'deactivate' : 'activate' %> this department?')">
                                                    <span class="text-sm sm:text-base"><i class="fas fa-power-off"></i></span>
                                                </button>
                                            </form>
                                            <form method="POST" action="/departments/<%= department.department_code %>/delete" style="display: inline-block;">
                                                <input type="hidden" name="dev-csrf-token" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : 'dev-csrf-token' %>">
                                                <button type="submit"
                                                        class="inline-flex items-center px-2 sm:px-3 py-2 bg-red-500 text-white rounded-md hover:bg-red-600 transition-all duration-200 shadow-sm"
                                                        title="Delete"
                                                        onclick="return confirm('Are you sure you want to delete department <%= department.department_code %>?')">
                                                    <span class="text-sm sm:text-base"><i class="fas fa-trash-alt"></i></span>
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="6" class="px-6 py-4 text-center text-gray-500">
                                    No departments found.
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    
    <!-- Pagination -->
    <% if (typeof pagination !== 'undefined' && pagination.pages > 1) { %>
        <%- include('../partials/pagination', { pagination, query: typeof query !== 'undefined' ? query : {} }) %>
    <% } %>
</div>

<script>
    // Delete confirmation function removed - using form-based approach
    
    // Dynamic division loading based on company selection
    document.getElementById('company_code').addEventListener('change', function() {
        const companyCode = this.value;
        const divisionSelect = document.getElementById('division_code');
        const currentDivision = '<%= typeof query !== "undefined" && query.division_code || "" %>';
        
        // Clear existing options
        divisionSelect.innerHTML = '<option value="">All Divisions</option>';
        
        if (companyCode) {
            // Fetch divisions for selected company
            fetch('/api/companies/' + companyCode + '/divisions')
                .then(response => response.json())
                .then(data => {
                    if (data.success && data.data) {
                        data.data.forEach(division => {
                            const option = document.createElement('option');
                            option.value = division.division_code;
                            option.textContent = division.division_code + ' - ' + division.division_name;
                            
                            // Restore selection if it matches
                            if (division.division_code === currentDivision) {
                                option.selected = true;
                            }
                            
                            divisionSelect.appendChild(option);
                        });
                    }
                })
                .catch(error => console.error('Error loading divisions:', error));
        }
    });
    
    // Trigger change event on page load if company is pre-selected
    if (document.getElementById('company_code').value) {
        document.getElementById('company_code').dispatchEvent(new Event('change'));
    }
</script>
    </div>