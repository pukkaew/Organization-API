    <div class="space-y-6">
            <!-- Page Header -->
            <div class="bg-gradient-to-r from-blue-600 to-indigo-700 rounded-2xl shadow-2xl p-8 mb-6">
                <div class="flex items-center justify-between">
                    <div class="flex items-center space-x-4">
                        <div class="w-16 h-16 bg-white bg-opacity-20 rounded-xl flex items-center justify-center backdrop-blur-sm">
                            <span class="text-3xl text-white">🏢</span>
                        </div>
                        <div>
                            <h1 class="text-4xl font-bold text-white">
                                Companies
                            </h1>
                            <p class="text-lg text-blue-100 mt-1">Manage all companies in your organization</p>
                        </div>
                    </div>
                    <div>
                        <a href="/companies/new" class="group inline-flex items-center px-6 py-3 bg-white text-blue-600 font-semibold rounded-xl hover:bg-blue-50 transform hover:scale-105 transition-all duration-200 shadow-lg">
                            <span class="text-xl mr-2">➕</span>
                            Add New Company
                            <span class="ml-2 group-hover:translate-x-1 transition-transform">→</span>
                        </a>
                    </div>
                </div>
            </div>
            
            <!-- Filters Section -->
            <div class="bg-gradient-to-r from-gray-50 to-blue-50 rounded-xl shadow-lg p-6 mb-6">
                <div class="flex items-center space-x-3 mb-4">
                    <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center">
                        <span class="text-white text-sm">🔍</span>
                    </div>
                    <h2 class="text-xl font-bold text-gray-800">
                        Search & Filter
                    </h2>
                </div>
                <form method="get" action="/companies" class="grid grid-cols-1 gap-4 sm:grid-cols-3">
                    <div>
                        <label for="search" class="block text-sm font-medium text-gray-700 mb-1">Search</label>
                        <div class="relative">
                            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                <i class="fas fa-search text-gray-400"></i>
                            </div>
                            <input type="text" 
                                   name="search" 
                                   id="search" 
                                   value="<%= typeof filters !== 'undefined' && filters.search || '' %>"
                                   placeholder="Search by code or name..."
                                   class="block w-full pl-10 pr-3 py-2 border border-gray-300 rounded-lg  focus:border-blue-500 sm:text-sm">
                        </div>
                    </div>
                    
                    <div>
                        <label for="is_active" class="block text-sm font-medium text-gray-700 mb-1">Status</label>
                        <select name="is_active" id="is_active" class="block w-full px-3 py-2 border border-gray-300 rounded-lg  focus:border-blue-500 sm:text-sm">
                            <option value="">All Status</option>
                            <option value="true" <%= typeof filters !== 'undefined' && filters.is_active === 'true' ? 'selected' : '' %>>Active</option>
                            <option value="false" <%= typeof filters !== 'undefined' && filters.is_active === 'false' ? 'selected' : '' %>>Inactive</option>
                        </select>
                    </div>
                    
                    <div class="flex items-end">
                        <button type="submit" class="w-full inline-flex items-center justify-center px-4 py-2 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition-colors">
                            <i class="fas fa-search mr-2"></i>
                            Apply Filters
                        </button>
                    </div>
                </form>
            </div>
            
            <!-- Companies Table -->
            <div class="overflow-hidden shadow ring-1 ring-black ring-opacity-5 md:rounded-lg">
                <div class="overflow-x-auto">
                    <table class="min-w-full divide-y divide-gray-200">
                        <thead class="bg-gradient-to-r from-gray-700 to-gray-800 text-white">
                            <tr>
                                <th scope="col" class="px-3 sm:px-6 py-4 sm:py-5 text-left text-xs font-bold text-white uppercase tracking-wider">
                                    <span class="text-base mr-2">🏢</span><span class="hidden sm:inline">Company</span> Code
                                </th>
                                <th scope="col" class="px-3 sm:px-6 py-4 sm:py-5 text-left text-xs font-bold text-white uppercase tracking-wider">
                                    <span class="text-base mr-2">🏛️</span><span class="hidden lg:inline">Thai</span> Name
                                </th>
                                <th scope="col" class="hidden md:table-cell px-3 sm:px-6 py-4 sm:py-5 text-left text-xs font-bold text-white uppercase tracking-wider">
                                    <span class="text-base mr-2">🌍</span>English Name
                                </th>
                                <th scope="col" class="hidden lg:table-cell px-3 sm:px-6 py-4 sm:py-5 text-left text-xs font-bold text-white uppercase tracking-wider">
                                    <span class="text-base mr-2">📋</span>Tax ID
                                </th>
                                <th scope="col" class="px-3 sm:px-6 py-4 sm:py-5 text-center text-xs font-bold text-white uppercase tracking-wider">
                                    <span class="text-base mr-2">📊</span><span class="hidden sm:inline">Status</span>
                                </th>
                                <th scope="col" class="px-3 sm:px-6 py-4 sm:py-5 text-center text-xs font-bold text-white uppercase tracking-wider">
                                    <span class="text-base mr-2">⚙️</span><span class="hidden sm:inline">Actions</span>
                                </th>
                            </tr>
                        </thead>
                        <tbody class="bg-white divide-y divide-gray-200">
                            <% if (typeof companies !== 'undefined' && companies.length > 0) { %>
                                <% companies.forEach(function(company, index) { %>
                                    <tr class="<%= index % 2 === 0 ? 'bg-white' : 'bg-gray-50' %> hover:bg-blue-50 hover:shadow-md transition-all duration-200">
                                        <td class="px-3 sm:px-6 py-3 sm:py-4 whitespace-nowrap">
                                            <div class="flex items-center">
                                                <div class="w-6 h-6 sm:w-8 sm:h-8 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-full flex items-center justify-center mr-2 sm:mr-3">
                                                    <span class="text-white text-xs sm:text-sm">🏢</span>
                                                </div>
                                                <div class="text-xs sm:text-sm font-semibold text-gray-900 truncate max-w-20 sm:max-w-none">
                                                    <%= company.company_code %>
                                                </div>
                                            </div>
                                        </td>
                                        <td class="px-3 sm:px-6 py-3 sm:py-4 whitespace-nowrap">
                                            <div class="text-xs sm:text-sm font-medium text-gray-900 truncate max-w-32 sm:max-w-none">
                                                <%= company.company_name_th %>
                                            </div>
                                        </td>
                                        <td class="hidden md:table-cell px-3 sm:px-6 py-3 sm:py-4 whitespace-nowrap">
                                            <div class="text-xs sm:text-sm text-gray-600 truncate">
                                                <%= company.company_name_en || '-' %>
                                            </div>
                                        </td>
                                        <td class="hidden lg:table-cell px-3 sm:px-6 py-3 sm:py-4 whitespace-nowrap">
                                            <div class="text-xs sm:text-sm text-gray-600 font-mono">
                                                <%= company.tax_id || '-' %>
                                            </div>
                                        </td>
                                        <td class="px-3 sm:px-6 py-3 sm:py-4 whitespace-nowrap text-center">
                                            <% if (company.is_active) { %>
                                                <span class="inline-flex items-center px-1.5 sm:px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                                    <i class="fas fa-check-circle mr-1"></i>
                                                    <span class="hidden sm:inline">Active</span>
                                                </span>
                                            <% } else { %>
                                                <span class="inline-flex items-center px-1.5 sm:px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-600">
                                                    <i class="fas fa-times-circle mr-1"></i>
                                                    <span class="hidden sm:inline">Inactive</span>
                                                </span>
                                            <% } %>
                                        </td>
                                        <td class="px-3 sm:px-6 py-3 sm:py-4 whitespace-nowrap text-center">
                                            <div class="flex justify-center space-x-1">
                                                <a href="/companies/<%= company.company_code %>" 
                                                   class="inline-flex items-center px-1.5 sm:px-2 py-1 bg-gradient-to-r from-blue-500 to-blue-600 text-white rounded-md hover:from-blue-600 hover:to-blue-700 transform hover:scale-105 transition-all duration-200 shadow-sm"
                                                   title="View Details">
                                                    <span class="text-xs sm:text-sm">👁️</span>
                                                </a>
                                                <a href="/companies/<%= company.company_code %>/edit" 
                                                   class="inline-flex items-center px-1.5 sm:px-2 py-1 bg-gradient-to-r from-yellow-500 to-orange-500 text-white rounded-md hover:from-yellow-600 hover:to-orange-600 transform hover:scale-105 transition-all duration-200 shadow-sm"
                                                   title="Edit">
                                                    <span class="text-xs sm:text-sm">✏️</span>
                                                </a>
                                                <button class="inline-flex items-center px-1.5 sm:px-2 py-1 bg-gradient-to-r from-red-500 to-red-600 text-white rounded-md hover:from-red-600 hover:to-red-700 transform hover:scale-105 transition-all duration-200 shadow-sm"
                                                        title="Delete"
                                                        onclick="confirmDelete('<%= company.company_code %>')">
                                                    <span class="text-xs sm:text-sm">🗑️</span>
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="6" class="px-6 py-12">
                                        <div class="text-center">
                                            <div class="w-16 h-16 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4">
                                                <i class="fas fa-building text-2xl text-gray-400"></i>
                                            </div>
                                            <h3 class="text-lg font-medium text-gray-900 mb-2">No companies found</h3>
                                            <p class="text-gray-500 mb-4">Get started by creating your first company.</p>
                                            <a href="/companies/new" class="inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
                                                <i class="fas fa-plus mr-2"></i>
                                                Add Company
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            
            <!-- Pagination -->
            <% if (typeof pagination !== 'undefined' && pagination.pages > 1) { %>
                <%- include('../partials/pagination', { pagination, query: typeof query !== 'undefined' ? query : {} }) %>
            <% } %>
    </div>

<script>
    // Delete confirmation
    function confirmDelete(companyCode) {
        if (confirm('Are you sure you want to delete company ' + companyCode + '?\n\nThis will delete all related data including:\n- Branches\n- Divisions\n- Departments')) {
            const form = document.createElement('form');
            form.method = 'POST';
            form.action = '/companies/' + companyCode + '/delete';
            
            const csrfToken = document.querySelector('meta[name="csrf-token"]')?.content;
            if (csrfToken) {
                const input = document.createElement('input');
                input.type = 'hidden';
                input.name = '_csrf';
                input.value = csrfToken;
                form.appendChild(input);
            }
            
            document.body.appendChild(form);
            form.submit();
        }
    }
</script>