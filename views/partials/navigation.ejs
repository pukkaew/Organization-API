<!-- Modern Navigation Bar -->
<nav class="bg-white shadow-lg sticky top-0 z-50 border-b border-gray-100">
    <div class="container mx-auto px-4 lg:px-6">
        <div class="flex items-center h-16 justify-between">
            <!-- Logo & Brand -->
            <div class="flex items-center space-x-4">
                <div class="w-12 h-12 bg-gradient-ruxchai rounded-xl flex items-center justify-center shadow-lg hover:shadow-xl transition-all duration-300 hover:scale-105">
                    <span class="text-white text-xl font-bold">🏢</span>
                </div>
                <div class="hidden sm:block">
                    <h1 class="text-xl font-bold text-ruxchai-blue leading-tight">Ruxchai Cold Storage</h1>
                    <p class="text-sm text-ruxchai-grey">ระบบจัดการโครงสร้างองค์กร</p>
                </div>
            </div>

            <!-- Main Navigation Menu -->
            <div class="hidden lg:flex items-center space-x-2 ml-8 fast-nav">
                <a href="/" class="nav-item fast-nav <%= typeof activeMenu !== 'undefined' && activeMenu === 'dashboard' ? 'active' : '' %>">
                    <span class="nav-icon">📊</span>
                    <span class="nav-text">Dashboard</span>
                </a>
                <a href="/companies" class="nav-item fast-nav <%= typeof activeMenu !== 'undefined' && activeMenu === 'companies' ? 'active' : '' %>">
                    <span class="nav-icon">🏢</span>
                    <span class="nav-text">บริษัท</span>
                </a>
                <a href="/branches" class="nav-item fast-nav <%= typeof activeMenu !== 'undefined' && activeMenu === 'branches' ? 'active' : '' %>">
                    <span class="nav-icon">🏪</span>
                    <span class="nav-text">สาขา</span>
                </a>
                <a href="/divisions" class="nav-item fast-nav <%= typeof activeMenu !== 'undefined' && activeMenu === 'divisions' ? 'active' : '' %>">
                    <span class="nav-icon">🏛️</span>
                    <span class="nav-text">ฝ่าย</span>
                </a>
                <a href="/departments" class="nav-item fast-nav <%= typeof activeMenu !== 'undefined' && activeMenu === 'departments' ? 'active' : '' %>">
                    <span class="nav-icon">👥</span>
                    <span class="nav-text">แผนก</span>
                </a>
                <a href="/api-keys" class="nav-item fast-nav <%= typeof activeMenu !== 'undefined' && activeMenu === 'api-keys' ? 'active' : '' %>">
                    <span class="nav-icon">🔐</span>
                    <span class="nav-text">API Keys</span>
                </a>
            </div>

            <!-- User Info & Actions -->
            <div class="flex items-center space-x-3">
                <% if (typeof user !== 'undefined' && user) { %>
                    <div class="hidden md:flex items-center space-x-3 bg-gradient-to-r from-blue-50 to-indigo-50 px-4 py-2 rounded-full border border-blue-200">
                        <div class="w-8 h-8 bg-ruxchai-blue rounded-full flex items-center justify-center">
                            <span class="text-white text-sm font-bold">👤</span>
                        </div>
                        <div class="hidden lg:block">
                            <p class="text-sm font-semibold text-gray-800"><%= user.username || 'User' %></p>
                            <p class="text-xs text-gray-500"><%= user.role === 'admin' ? 'ผู้ดูแลระบบ' : 'ผู้ใช้งาน' %></p>
                        </div>
                    </div>
                <% } %>
                
                <a href="/logout" class="logout-btn">
                    <span class="logout-icon">🚪</span>
                    <span class="hidden md:inline logout-text">ออกจากระบบ</span>
                </a>

                <!-- Mobile Menu Button -->
                <button type="button" class="lg:hidden mobile-menu-btn" id="mobile-menu-button">
                    <span class="hamburger-icon">☰</span>
                </button>
            </div>
        </div>
    </div>
    
    <!-- Mobile menu -->
    <div class="lg:hidden hidden" id="mobile-menu">
        <div class="px-4 pt-4 pb-4 space-y-2 bg-gradient-to-br from-gray-50 to-gray-100 border-t border-gray-200">
            <a href="/" class="mobile-nav-item fast-nav <%= typeof activeMenu !== 'undefined' && activeMenu === 'dashboard' ? 'active' : '' %>">
                <span class="mobile-nav-icon">📊</span>
                <span class="mobile-nav-text">Dashboard</span>
            </a>
            <a href="/companies" class="mobile-nav-item fast-nav <%= typeof activeMenu !== 'undefined' && activeMenu === 'companies' ? 'active' : '' %>">
                <span class="mobile-nav-icon">🏢</span>
                <span class="mobile-nav-text">บริษัท</span>
            </a>
            <a href="/branches" class="mobile-nav-item fast-nav <%= typeof activeMenu !== 'undefined' && activeMenu === 'branches' ? 'active' : '' %>">
                <span class="mobile-nav-icon">🏪</span>
                <span class="mobile-nav-text">สาขา</span>
            </a>
            <a href="/divisions" class="mobile-nav-item fast-nav <%= typeof activeMenu !== 'undefined' && activeMenu === 'divisions' ? 'active' : '' %>">
                <span class="mobile-nav-icon">🏛️</span>
                <span class="mobile-nav-text">ฝ่าย</span>
            </a>
            <a href="/departments" class="mobile-nav-item fast-nav <%= typeof activeMenu !== 'undefined' && activeMenu === 'departments' ? 'active' : '' %>">
                <span class="mobile-nav-icon">👥</span>
                <span class="mobile-nav-text">แผนก</span>
            </a>
            <a href="/api-keys" class="mobile-nav-item fast-nav <%= typeof activeMenu !== 'undefined' && activeMenu === 'api-keys' ? 'active' : '' %>">
                <span class="mobile-nav-icon">🔐</span>
                <span class="mobile-nav-text">API Keys</span>
            </a>
            
            <div class="border-t border-gray-300 pt-4 mt-4">
                <% if (typeof user !== 'undefined' && user) { %>
                    <div class="flex items-center space-x-3 px-4 py-3 mb-3 bg-gradient-to-r from-blue-50 to-indigo-50 rounded-xl border border-blue-200">
                        <div class="w-10 h-10 bg-ruxchai-blue rounded-full flex items-center justify-center">
                            <span class="text-white font-bold">👤</span>
                        </div>
                        <div>
                            <p class="text-sm font-semibold text-gray-800"><%= user.username || 'User' %></p>
                            <p class="text-xs text-gray-500"><%= user.role === 'admin' ? 'ผู้ดูแลระบบ' : 'ผู้ใช้งาน' %></p>
                        </div>
                    </div>
                <% } %>
                <a href="/logout" class="mobile-logout-item">
                    <span class="mobile-logout-icon">🚪</span>
                    <span class="mobile-logout-text">ออกจากระบบ</span>
                </a>
            </div>
        </div>
    </div>
</nav>

<style>
    /* Navigation Styles */
    .nav-item {
        display: flex;
        align-items: center;
        space-x: 2;
        padding: 0.75rem 1rem;
        border-radius: 0.75rem;
        transition: all 0.15s ease;
        text-decoration: none;
        color: #6b7280;
        font-weight: 500;
        font-size: 0.875rem;
        position: relative;
        overflow: hidden;
    }
    
    .nav-item:hover {
        background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
        color: #0090D3;
        transform: translateY(-1px);
        box-shadow: 0 4px 12px rgba(0, 144, 211, 0.15);
    }
    
    .nav-item.active {
        background: linear-gradient(135deg, #0090D3 0%, #047685 100%);
        color: white;
        box-shadow: 0 4px 15px rgba(0, 144, 211, 0.4);
    }
    
    .nav-item.active:hover {
        background: linear-gradient(135deg, #0080c7 0%, #036975 100%);
        transform: translateY(-1px);
        box-shadow: 0 6px 20px rgba(0, 144, 211, 0.5);
    }
    
    .nav-icon {
        font-size: 1.125rem;
        margin-right: 0.5rem;
    }
    
    .nav-text {
        font-size: 0.875rem;
        font-weight: 500;
    }
    
    /* Logout Button */
    .logout-btn {
        display: flex;
        align-items: center;
        space-x: 2;
        padding: 0.5rem 1rem;
        background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
        color: white;
        border-radius: 0.75rem;
        text-decoration: none;
        font-weight: 500;
        font-size: 0.875rem;
        transition: all 0.3s ease;
        box-shadow: 0 2px 8px rgba(239, 68, 68, 0.3);
    }
    
    .logout-btn:hover {
        background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
        transform: translateY(-1px);
        box-shadow: 0 4px 12px rgba(239, 68, 68, 0.4);
        color: white;
    }
    
    .logout-icon {
        font-size: 1rem;
        margin-right: 0.25rem;
    }
    
    .logout-text {
        font-size: 0.875rem;
    }
    
    /* Mobile Menu Button */
    .mobile-menu-btn {
        padding: 0.5rem;
        background: #f3f4f6;
        border: 1px solid #e5e7eb;
        border-radius: 0.5rem;
        transition: all 0.3s ease;
    }
    
    .mobile-menu-btn:hover {
        background: #e5e7eb;
        border-color: #d1d5db;
    }
    
    .hamburger-icon {
        font-size: 1.25rem;
        color: #374151;
    }
    
    /* Mobile Navigation Items */
    .mobile-nav-item {
        display: flex;
        align-items: center;
        space-x: 3;
        padding: 0.875rem 1rem;
        background: white;
        border-radius: 0.75rem;
        text-decoration: none;
        color: #374151;
        font-weight: 500;
        font-size: 0.875rem;
        transition: all 0.3s ease;
        border: 1px solid #e5e7eb;
        margin-bottom: 0.5rem;
    }
    
    .mobile-nav-item:hover {
        background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
        color: #0090D3;
        border-color: #0090D3;
        transform: translateX(4px);
    }
    
    .mobile-nav-item.active {
        background: linear-gradient(135deg, #0090D3 0%, #047685 100%);
        color: white;
        border-color: #0090D3;
        box-shadow: 0 4px 12px rgba(0, 144, 211, 0.3);
    }
    
    .mobile-nav-icon {
        font-size: 1.25rem;
        margin-right: 0.75rem;
    }
    
    .mobile-nav-text {
        font-size: 0.875rem;
        font-weight: 500;
    }
    
    /* Mobile Logout */
    .mobile-logout-item {
        display: flex;
        align-items: center;
        space-x: 3;
        padding: 0.875rem 1rem;
        background: linear-gradient(135deg, #fef2f2 0%, #fee2e2 100%);
        border: 1px solid #fecaca;
        border-radius: 0.75rem;
        text-decoration: none;
        color: #dc2626;
        font-weight: 500;
        font-size: 0.875rem;
        transition: all 0.3s ease;
    }
    
    .mobile-logout-item:hover {
        background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
        color: white;
        border-color: #dc2626;
        transform: translateX(4px);
    }
    
    .mobile-logout-icon {
        font-size: 1.25rem;
        margin-right: 0.75rem;
    }
    
    .mobile-logout-text {
        font-size: 0.875rem;
        font-weight: 500;
    }
    
    /* Animation for mobile menu */
    #mobile-menu {
        transition: all 0.3s ease;
    }
    
    #mobile-menu.hidden {
        opacity: 0;
        transform: translateY(-10px);
    }
    
    #mobile-menu:not(.hidden) {
        opacity: 1;
        transform: translateY(0);
    }
</style>



