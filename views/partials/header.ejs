<!-- Path: /views/partials/header.ejs -->
<!DOCTYPE html>
<html lang="th" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Organization Structure Management System">
    <meta name="csrf-token" content="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
    
    <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %>Organization Management</title>
    
    <!-- Modern UI Framework -->
    <link rel="stylesheet" href="/assets/css/modern-ui.css">
    
    <!-- Font Awesome Icons - Simple and reliable -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" crossorigin="anonymous" referrerpolicy="no-referrer">
    
    <!-- CSS Order as per PDF Report -->
    <link rel="stylesheet" href="/css/variables.css">
    <link rel="stylesheet" href="/css/utilities-fixed.css">
    <link rel="stylesheet" href="/css/missing-styles.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/ruxchai-colors.css">
    
    <!-- Modern Icon System -->
    <link rel="stylesheet" href="/assets/css/icons.css">
    
    <!-- Performance CSS -->
    <link rel="stylesheet" href="/css/ultra-fast.css">
    
    <!-- Remove all underlines from links and buttons -->
    <link rel="stylesheet" href="/css/no-underline.css">
    
    <!-- Status Badge High Contrast Fix -->
    <link rel="stylesheet" href="/css/status-badge-fix.css">
    
    <!-- Table Header High Contrast Fix -->
    <link rel="stylesheet" href="/css/table-header-fix.css">
    
    <!-- Minimal inline styles -->
    <style>
        body { 
            font-family: 'Segoe UI', 'Noto Sans Thai', 'Helvetica', 'Arial', sans-serif;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }
    </style>
    
    <!-- Chart.js (if needed) -->
    <% if (typeof includeChartJS !== 'undefined' && includeChartJS) { %>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <% } %>
    
    <!-- Local JavaScript -->
    <script src="/assets/js/app.js"></script>
</head>
<body class="min-h-screen bg-white">
    <div class="min-h-screen flex flex-col">