<!-- Path: /views/partials/footer.ejs -->
        <!-- Footer -->
        <footer class="bg-white border-t border-gray-200 mt-auto shadow-sm">
            <div class="mx-auto max-w-full px-4 sm:px-6 lg:px-8">
                <div class="py-6 text-center">
                    <div class="flex items-center justify-center mb-4">
                        <div class="w-8 h-8 bg-ruxchai-blue rounded-lg flex items-center justify-center mr-3">
                            <i class="fas fa-building text-white text-lg"></i>
                        </div>
                        <p class="text-sm font-medium text-gray-700">
                            &copy; <%= new Date().getFullYear() %> Ruxchai Cold Storage Organization Management System. All rights reserved.
                        </p>
                    </div>
                    <div class="flex items-center justify-center space-x-4">
                        <a href="/docs" class="inline-flex items-center px-4 py-2 bg-ruxchai-blue text-white rounded-lg hover:bg-ruxchai-blue-dark transition-colors duration-200 shadow-sm text-sm font-medium">
                            <i class="fas fa-book text-sm mr-2" aria-hidden="true"></i>
                            API Documentation
                        </a>
                        <a href="mailto:support@ruxchai.com" class="inline-flex items-center px-4 py-2 bg-ruxchai-green text-white rounded-lg hover:bg-ruxchai-green-dark transition-colors duration-200 shadow-sm text-sm font-medium">
                            <i class="fas fa-envelope text-sm mr-2" aria-hidden="true"></i>
                            Support
                        </a>
                    </div>
                </div>
            </div>
        </footer>
    </div>
    
    <!-- Main JavaScript -->
    <script src="/js/app.js"></script>
    
    <!-- Performance Toggle -->
    <script src="/assets/js/performance-toggle.js"></script>
    
    <!-- Essential interactions -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Remove loading placeholder from tables
            const tables = document.querySelectorAll('table');
            tables.forEach(table => {
                setTimeout(() => {
                    table.classList.remove('loading-placeholder');
                }, 300);
            });
        });
    </script>
    
    <!-- Page specific scripts can be added here via a variable -->
    <% if (typeof pageScripts !== 'undefined') { %>
        <%- pageScripts %>
    <% } %>
</body>
</html>