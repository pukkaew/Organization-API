<!-- Path: /views/api-keys/show.ejs -->
<%- include('../partials/header', {
    title: 'API Key Created Successfully',
    includeChartJS: false
}) %>

<%- include('../partials/navigation', {
    activeMenu: 'api-keys',
    user: typeof user !== 'undefined' ? user : null
}) %>

<!-- Main Content -->
<main class="flex-1 py-4 sm:py-8">
    <div class="mx-auto max-w-full px-4 sm:px-6 lg:px-8">
        <div class="max-w-4xl mx-auto">
        <div class="bg-gradient-to-r from-emerald-400 via-green-500 to-teal-600 rounded-xl p-6 mb-8 shadow-xl">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-white bg-opacity-20 rounded-xl flex items-center justify-center text-2xl">
                        <span class="text-white text-2xl">✅</span>
                    </div>
                </div>
                <div class="ml-4">
                    <h3 class="text-xl font-bold text-white">
                        API Key Created Successfully!
                    </h3>
                    <p class="mt-1 text-sm text-green-100">
                        Your API key has been created. This is the only time you'll see the full key.
                    </p>
                </div>
            </div>
        </div>
    
        <div class="bg-white shadow-xl rounded-xl overflow-hidden">
            <!-- Header with Gradient Background -->
            <div class="bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-700 px-6 py-8 text-white">
                <div class="flex items-center">
                    <div class="w-16 h-16 bg-white bg-opacity-20 rounded-xl flex items-center justify-center text-3xl mr-4">
                        <span class="text-white text-3xl drop-shadow-lg">🔑</span>
                    </div>
                    <div>
                        <h3 class="text-2xl font-bold text-white">
                            API Key Details
                        </h3>
                        <p class="mt-1 text-sm text-purple-100">
                            Save this information securely. You won't be able to see the full API key again.
                        </p>
                    </div>
                </div>
            </div>
            <div class="p-6 bg-gradient-to-br from-gray-50 to-indigo-50">
                <dl class="grid grid-cols-1 gap-6 sm:grid-cols-2">
                    <div class="bg-gradient-to-br from-blue-50 to-indigo-100 p-6 rounded-xl shadow-lg border border-blue-200 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
                        <dt class="text-sm font-medium text-gray-600 flex items-center mb-3">
                            <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center mr-3">
                                <div class="w-8 h-8 bg-gradient-to-br from-purple-400 to-purple-600 rounded-lg flex items-center justify-center shadow-md">
                                    <span class="text-white text-sm">📱</span>
                                </div>
                            </div>
                            Application Name
                        </dt>
                        <dd class="text-lg font-bold text-gray-900 bg-white px-3 py-2 rounded-lg shadow-sm">
                            <%= apiKey.app_name %>
                        </dd>
                    </div>
                    
                    <div class="bg-gradient-to-br from-emerald-50 to-teal-100 p-6 rounded-xl shadow-lg border border-emerald-200 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
                        <dt class="text-sm font-medium text-gray-600 flex items-center mb-3">
                            <div class="w-8 h-8 bg-gradient-to-br from-emerald-500 to-teal-600 rounded-lg flex items-center justify-center mr-3">
                                <div class="w-8 h-8 bg-gradient-to-br from-purple-400 to-purple-600 rounded-lg flex items-center justify-center shadow-md">
                                    <span class="text-white text-sm">📝</span>
                                </div>
                            </div>
                            Description
                        </dt>
                        <dd class="text-sm text-gray-900 bg-white px-3 py-2 rounded-lg shadow-sm">
                            <%= apiKey.description || '-' %>
                        </dd>
                    </div>
                    
                    <div class="bg-gradient-to-br from-purple-50 to-pink-100 p-6 rounded-xl shadow-lg border border-purple-200 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
                        <dt class="text-sm font-medium text-gray-600 flex items-center mb-3">
                            <div class="w-8 h-8 bg-gradient-to-br from-purple-500 to-pink-600 rounded-lg flex items-center justify-center mr-3">
                                <div class="w-8 h-8 bg-gradient-to-br from-purple-400 to-purple-600 rounded-lg flex items-center justify-center shadow-md">
                                    <span class="text-white text-sm">🛡️</span>
                                </div>
                            </div>
                            Permissions
                        </dt>
                        <dd class="mt-1">
                            <% if (apiKey.permissions === 'read') { %>
                                <span class="inline-flex items-center px-4 py-2 rounded-xl text-sm font-bold bg-gradient-to-r from-blue-400 to-indigo-500 text-white shadow-lg">
                                    <span class="mr-2">👁️</span>Read Only
                                </span>
                            <% } else if (apiKey.permissions === 'write') { %>
                                <span class="inline-flex items-center px-4 py-2 rounded-xl text-sm font-bold bg-gradient-to-r from-yellow-400 to-orange-500 text-white shadow-lg">
                                    <span class="mr-2">✏️</span>Write Only
                                </span>
                            <% } else { %>
                                <span class="inline-flex items-center px-4 py-2 rounded-xl text-sm font-bold bg-gradient-to-r from-green-400 to-emerald-500 text-white shadow-lg animate-pulse">
                                    <span class="mr-2">✅</span>Read & Write
                                </span>
                            <% } %>
                        </dd>
                    </div>
                    
                    <div class="bg-gradient-to-br from-orange-50 to-amber-100 p-6 rounded-xl shadow-lg border border-orange-200 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
                        <dt class="text-sm font-medium text-gray-600 flex items-center mb-3">
                            <div class="w-8 h-8 bg-gradient-to-br from-orange-500 to-amber-600 rounded-lg flex items-center justify-center mr-3">
                                <div class="w-8 h-8 bg-gradient-to-br from-purple-400 to-purple-600 rounded-lg flex items-center justify-center shadow-md">
                                    <span class="text-white text-sm">⏰</span>
                                </div>
                            </div>
                            Expires
                        </dt>
                        <dd class="text-lg font-bold text-gray-900 bg-white px-3 py-2 rounded-lg shadow-sm">
                            <% if (apiKey.expires_date) { %>
                                <%= new Date(apiKey.expires_date).toLocaleDateString() %>
                            <% } else { %>
                                Never
                            <% } %>
                        </dd>
                    </div>
                </dl>
                
                <!-- API Key Section -->
                <div class="mt-8 bg-gradient-to-br from-red-50 to-pink-100 p-6 rounded-xl shadow-lg border border-red-200">
                    <div class="text-sm font-medium text-gray-600 flex items-center mb-4">
                        <div class="w-8 h-8 bg-gradient-to-br from-red-500 to-pink-600 rounded-lg flex items-center justify-center mr-3">
                            <div class="w-8 h-8 bg-gradient-to-br from-red-400 to-red-600 rounded-lg flex items-center justify-center shadow-md">
                                <span class="text-white text-sm">🔒</span>
                            </div>
                        </div>
                        API Key (Copy and save this now!)
                    </div>
                    <div class="mt-1">
                        <div class="flex items-center space-x-3">
                            <input type="text" 
                                   id="apiKeyField" 
                                   value="<%= actualKey %>" 
                                   readonly
                                   class="flex-1 font-mono text-sm bg-white p-4 rounded-lg border border-gray-300 select-all break-all shadow-sm">
                            <button type="button"
                                    onclick="copyApiKey()"
                                    class="group inline-flex items-center px-6 py-4 bg-gradient-to-r from-blue-500 to-indigo-600 text-white rounded-lg hover:from-blue-600 hover:to-indigo-700 transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl">
                                <span class="mr-2 transition-transform group-hover:rotate-12">📋</span>
                                Copy
                            </button>
                        </div>
                        <div class="mt-4 p-4 bg-gradient-to-r from-red-100 to-pink-100 rounded-lg border border-red-200">
                            <p class="text-sm text-red-700 flex items-center">
                                <span class="text-lg mr-2">⚠️</span>
                                This is your secret API key. Keep it secure and never share it publicly or commit it to version control.
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    
        <div class="mt-8 bg-gradient-to-br from-yellow-50 to-amber-100 p-6 rounded-xl shadow-lg border border-yellow-200">
            <div class="flex items-start">
                <div class="flex-shrink-0">
                    <div class="w-10 h-10 bg-gradient-to-br from-yellow-400 to-amber-500 rounded-xl flex items-center justify-center text-xl">
                        <span>💡</span>
                    </div>
                </div>
                <div class="ml-4 flex-1">
                    <h3 class="text-lg font-bold text-yellow-900 mb-3">
                        How to use your API key
                    </h3>
                    <div class="text-sm text-yellow-800 space-y-4">
                        <p class="font-medium">Include the API key in your request headers:</p>
                        <div class="bg-gradient-to-r from-gray-800 to-gray-900 text-green-300 p-4 rounded-lg font-mono text-sm overflow-x-auto shadow-inner">
                            X-API-Key: <%= actualKey %>
                        </div>
                        <p class="font-medium mt-4">
                            Example using cURL:
                        </p>
                        <div class="bg-gradient-to-r from-gray-800 to-gray-900 text-blue-300 p-4 rounded-lg font-mono text-sm overflow-x-auto shadow-inner">
                            curl -H "X-API-Key: <%= actualKey %>" <%= typeof request !== 'undefined' ? request.protocol + '://' + request.get('host') : 'https://example.com' %>/api/v1/companies
                        </div>
                    </div>
                </div>
            </div>
        </div>
    
        <div class="mt-8 flex flex-col sm:flex-row gap-4 sm:justify-between">
            <a href="/api-keys" class="group inline-flex items-center justify-center px-6 py-3 bg-white bg-opacity-20 backdrop-blur-sm text-gray-700 rounded-lg hover:bg-gray-50 transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl border border-gray-200">
                <span class="mr-2 transition-transform group-hover:-translate-x-1">←</span>
                Back to API Keys
            </a>
            <a href="/docs" class="group inline-flex items-center justify-center px-6 py-3 bg-gradient-to-r from-blue-500 to-indigo-600 text-white rounded-lg hover:from-blue-600 hover:to-indigo-700 transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl">
                <span class="mr-2 transition-transform group-hover:rotate-12">📚</span>
                View API Documentation
            </a>
        </div>
        </div>
    </div>
</main>

<%- include('../partials/footer', {
    pageScripts: `
    <script>
        // Auto-select the API key field when page loads
        document.addEventListener('DOMContentLoaded', function() {
            const apiKeyField = document.getElementById('apiKeyField');
            if (apiKeyField) {
                apiKeyField.select();
            }
        });
        
        // Copy API key to clipboard
        function copyApiKey() {
            const apiKeyField = document.getElementById('apiKeyField');
            const button = event.currentTarget;
            
            apiKeyField.select();
            apiKeyField.setSelectionRange(0, 99999); // For mobile devices
            
            navigator.clipboard.writeText(apiKeyField.value).then(() => {
                const originalHTML = button.innerHTML;
                button.innerHTML = '<span class="mr-2">✅</span>Copied!';
                button.classList.add('bg-green-600', 'hover:bg-green-700');
                button.classList.remove('bg-blue-600', 'hover:bg-blue-700');
                
                setTimeout(() => {
                    button.innerHTML = originalHTML;
                    button.classList.remove('bg-green-600', 'hover:bg-green-700');
                    button.classList.add('bg-blue-600', 'hover:bg-blue-700');
                }, 2000);
            }).catch(err => {
                console.error('Failed to copy:', err);
                alert('Failed to copy API key. Please copy it manually.');
            });
        }
    </script>
    `
}) %>