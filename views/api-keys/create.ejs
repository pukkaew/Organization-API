<!-- Path: /views/api-keys/create.ejs -->
<%- include('../partials/header', { 
    title: 'Create API Key',
    includeChartJS: false
}) %>
<%- include('../partials/navigation', { 
    activeMenu: 'api-keys',
    user: typeof user !== 'undefined' ? user : null
}) %>

<!-- Main Content -->
<main class="flex-1 py-8">
    <div class="mx-auto max-w-full px-4 sm:px-6 lg:px-8">
        <div class="max-w-3xl mx-auto">
            <div class="bg-gradient-to-br from-white to-gray-50 rounded-2xl shadow-2xl border border-gray-100">
                <%- include('../partials/form-header', { 
                    title: 'Create API Key',
                    icon: 'key',
                    iconColor: 'purple',
                    backUrl: '/api-keys'
                }) %>
                
                <%- include('../partials/form-errors', { 
                    error: typeof error !== 'undefined' ? error : null,
                    validationErrors: typeof validationErrors !== 'undefined' ? validationErrors : null
                }) %>
                
                <form method="post" action="/api-keys" class="form-section">
                    <% if (typeof csrfToken !== 'undefined') { %>
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <% } %>
                
                <div class="space-y-4">
                    <!-- Application Name -->
                    <div class="form-group-enhanced">
                        <label for="app_name" class="block text-sm font-medium text-gray-800 font-semibold flex items-center">
                            <div class="w-6 h-6 bg-gradient-to-br from-purple-400 to-purple-600 rounded-lg flex items-center justify-center mr-2 shadow-sm">
                                <i class="fas fa-mobile-alt text-white text-xs" aria-hidden="true"></i>
                            </div>
                            Application Name <span class="text-red-500">*</span>
                        </label>
                        <input type="text" 
                               id="app_name" 
                               name="app_name" 
                               required
                               maxlength="100"
                               class="form-input-enhanced"
                               placeholder="e.g., Mobile App, Web Portal">
                        <p class="mt-1 text-sm text-gray-500">
                            A descriptive name for the application using this API key
                        </p>
                    </div>
                    
                    <!-- Description -->
                    <div class="form-group-enhanced">
                        <label for="description" class="block text-sm font-medium text-gray-800 font-semibold flex items-center">
                            <div class="w-6 h-6 bg-gradient-to-br from-purple-400 to-purple-600 rounded-lg flex items-center justify-center mr-2 shadow-sm">
                                <i class="fas fa-edit text-white text-xs" aria-hidden="true"></i>
                            </div>
                            Description
                        </label>
                        <textarea id="description" 
                                  name="description" 
                                  rows="3"
                                  maxlength="500"
                                  class="form-input-enhanced"
                                  placeholder="Optional description of what this API key is used for"></textarea>
                    </div>
                    
                    <!-- Permissions -->
                    <div class="form-group-enhanced">
                        <label class="block text-sm font-medium text-gray-800 font-semibold flex items-center">
                            <div class="w-6 h-6 bg-gradient-to-br from-purple-400 to-purple-600 rounded-lg flex items-center justify-center mr-2 shadow-sm">
                                <i class="fas fa-lock text-white text-xs" aria-hidden="true"></i>
                            </div>
                            Permissions <span class="text-red-500">*</span>
                        </label>
                        <div class="mt-2 space-y-4">
                            <label class="flex items-start">
                                <input type="radio" 
                                       name="permissions" 
                                       value="read" 
                                       required
                                       class="mt-1 text-primary-600 ">
                                <div class="ml-3">
                                    <span class="block text-sm font-medium text-gray-700">Read Only</span>
                                    <span class="block text-sm text-gray-500">
                                        Can only view data (GET requests)
                                    </span>
                                </div>
                            </label>
                            
                            <label class="flex items-start">
                                <input type="radio" 
                                       name="permissions" 
                                       value="write"
                                       class="mt-1 text-primary-600 ">
                                <div class="ml-3">
                                    <span class="block text-sm font-medium text-gray-700">Write Only</span>
                                    <span class="block text-sm text-gray-500">
                                        Can only create/update/delete data (POST, PUT, DELETE requests)
                                    </span>
                                </div>
                            </label>
                            
                            <label class="flex items-start">
                                <input type="radio" 
                                       name="permissions" 
                                       value="read_write"
                                       checked
                                       class="mt-1 text-primary-600 ">
                                <div class="ml-3">
                                    <span class="block text-sm font-medium text-gray-700">Read & Write</span>
                                    <span class="block text-sm text-gray-500">
                                        Full access to all API endpoints
                                    </span>
                                </div>
                            </label>
                        </div>
                    </div>
                    
                    <!-- Expiration -->
                    <div class="form-group-enhanced">
                        <label for="expires_date" class="block text-sm font-medium text-gray-800 font-semibold flex items-center">
                            <div class="w-6 h-6 bg-gradient-to-br from-purple-400 to-purple-600 rounded-lg flex items-center justify-center mr-2 shadow-sm">
                                <i class="fas fa-calendar text-white text-xs" aria-hidden="true"></i>
                            </div>
                            Expiration Date
                        </label>
                        <input type="date" 
                               id="expires_date" 
                               name="expires_date"
                               min="<%= new Date().toISOString().split('T')[0] %>"
                               class="form-input-enhanced">
                        <p class="mt-1 text-sm text-gray-500">
                            Leave empty for no expiration
                        </p>
                    </div>
                    
                    <!-- Rate Limiting -->
                    <div class="form-group-enhanced">
                        <label for="rate_limit" class="block text-sm font-medium text-gray-800 font-semibold flex items-center">
                            <div class="w-6 h-6 bg-gradient-to-br from-purple-400 to-purple-600 rounded-lg flex items-center justify-center mr-2 shadow-sm">
                                <i class="fas fa-clock text-white text-xs" aria-hidden="true"></i>
                            </div>
                            Rate Limit (requests per hour)
                        </label>
                        <input type="number" 
                               id="rate_limit" 
                               name="rate_limit"
                               min="1"
                               max="10000"
                               value="1000"
                               class="form-input-enhanced">
                        <p class="mt-1 text-sm text-gray-500">
                            Maximum number of API requests allowed per hour
                        </p>
                    </div>
                    
                    <!-- IP Whitelist -->
                    <div class="form-group-enhanced">
                        <label for="ip_whitelist" class="block text-sm font-medium text-gray-800 font-semibold flex items-center">
                            <div class="w-6 h-6 bg-gradient-to-br from-purple-400 to-purple-600 rounded-lg flex items-center justify-center mr-2 shadow-sm">
                                <i class="fas fa-globe text-white text-xs" aria-hidden="true"></i>
                            </div>
                            IP Whitelist
                        </label>
                        <textarea id="ip_whitelist" 
                                  name="ip_whitelist" 
                                  rows="3"
                                  class="form-input-enhanced"
                                  placeholder="192.168.1.1&#10;10.0.0.0/24&#10;(one IP or CIDR per line)"></textarea>
                        <p class="mt-1 text-sm text-gray-500">
                            Optional: Restrict API key usage to specific IP addresses or ranges
                        </p>
                    </div>
                </div>
                
                <!-- Form Actions -->
                <div class="mt-8 pt-6 border-t border-gray-200 flex items-center justify-between">
                    <a href="/api-keys" class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 hover:bg-gray-100 transition-all duration-200">
                        <i class="fas fa-arrow-left mr-2" aria-hidden="true"></i>
                        Cancel
                    </a>
                    
                    <button type="submit" id="submit-btn" class="btn btn-primary">
                        <span class="btn-text">
                            <i class="fas fa-key mr-2" aria-hidden="true"></i>
                            สร้าง API Key
                        </span>
                        <span class="btn-loading hidden">
                            <span class="spinner mr-2"></span>
                            กำลังสร้าง...
                        </span>
                    </button>
                </div>
            </form>
        </div>
        
        <!-- Security Notice -->
        <div class="mt-6 bg-gradient-to-r from-cyan-50 to-blue-50 rounded-2xl p-6 border border-cyan-100 shadow-lg">
            <div class="flex">
                <div class="flex-shrink-0">
                    <i class="fas fa-shield-alt text-2xl text-cyan-500" aria-hidden="true"></i>
                </div>
                <div class="ml-4">
                    <h3 class="text-lg font-bold text-cyan-800 flex items-center">
                        <i class="fas fa-shield-alt mr-2" aria-hidden="true"></i>
                        Security Notice
                    </h3>
                    <div class="mt-3 text-sm text-cyan-700">
                        <ul class="space-y-2">
                            <li class="flex items-start">
                                <span class="text-cyan-500 mr-2 mt-0.5">•</span>
                                The API key will only be shown once after creation
                            </li>
                            <li class="flex items-start">
                                <span class="text-cyan-500 mr-2 mt-0.5">•</span>
                                Store the API key securely and never expose it in client-side code
                            </li>
                            <li class="flex items-start">
                                <span class="text-cyan-500 mr-2 mt-0.5">•</span>
                                Use HTTPS for all API requests
                            </li>
                            <li class="flex items-start">
                                <span class="text-cyan-500 mr-2 mt-0.5">•</span>
                                Rotate API keys regularly for better security
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
        </div>    
    </div>
</main>

<%- include('../partials/footer', {
    pageScripts: `
    <script>
    // Form submission loading state
    document.querySelector('form').addEventListener('submit', function() {
        const submitBtn = document.getElementById('submit-btn');
        const btnText = submitBtn.querySelector('.btn-text');
        const btnLoading = submitBtn.querySelector('.btn-loading');
        
        submitBtn.disabled = true;
        btnText.classList.add('hidden');
        btnLoading.classList.remove('hidden');
    });
    </script>
    `
}) %>