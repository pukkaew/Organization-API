<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Ruxchai Cold Storage</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0090D3 0%, #047685 25%, #1e3a8a 50%, #3AAA35 75%, #0090D3 100%);
            background-size: 400% 400%;
            animation: dynamicBg 15s ease infinite;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        
        @keyframes dynamicBg {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
        
        .login-container {
            width: 100%;
            max-width: 350px;
            animation: slideIn 0.8s ease-out;
        }
        
        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .logo {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .logo-icon {
            width: 90px;
            height: 90px;
            background: linear-gradient(135deg, #0090D3 0%, #3AAA35 50%, #047685 100%);
            border-radius: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 25px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            font-size: 36px;
            color: white;
        }
        
        .logo h1 {
            color: white;
            font-size: 28px;
            font-weight: bold;
            margin-bottom: 10px;
        }
        
        .logo p {
            color: rgba(255, 255, 255, 0.9);
            font-size: 16px;
        }
        
        .login-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 18px;
            padding: 35px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .welcome {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .welcome h2 {
            color: #333;
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 10px;
        }
        
        .welcome p {
            color: #666;
            font-size: 16px;
        }
        
        .form-group {
            margin-bottom: 24px;
            position: relative;
        }
        
        .form-input {
            width: 100%;
            padding: 15px 20px 15px 50px;
            border: 2px solid #e0e0e0;
            border-radius: 12px;
            font-size: 16px;
            transition: all 0.3s ease;
            background: #fff;
        }
        
        .form-input:focus {
            outline: none;
            border-color: #0090D3;
            box-shadow: 0 0 0 4px rgba(0, 144, 211, 0.1);
        }
        
        .input-icon {
            position: absolute;
            left: 18px;
            top: 50%;
            transform: translateY(-50%);
            color: #0090D3;
            font-size: 18px;
        }
        
        .checkbox-group {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 25px;
        }
        
        .checkbox-group input {
            margin-right: 10px;
            transform: scale(1.2);
        }
        
        .checkbox-group label {
            font-size: 14px;
            color: #666;
            cursor: pointer;
        }
        
        .login-btn {
            width: 100%;
            background: linear-gradient(135deg, #0090D3 0%, #047685 100%);
            color: white;
            padding: 15px;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 144, 211, 0.4);
        }
        
        .login-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(0, 144, 211, 0.6);
        }
        
        .login-btn:active {
            transform: translateY(0);
        }
        
        .login-btn:disabled {
            background: #ccc;
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }
        
        .demo-section {
            margin-top: 30px;
            padding-top: 25px;
            border-top: 1px solid #e0e0e0;
        }
        
        .demo-title {
            text-align: center;
            font-size: 14px;
            color: #666;
            margin-bottom: 18px;
            font-weight: 500;
        }
        
        .demo-creds {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 18px;
            font-size: 14px;
        }
        
        .demo-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 12px;
        }
        
        .demo-item:last-child {
            margin-bottom: 0;
        }
        
        .demo-label {
            font-weight: 600;
            color: #333;
        }
        
        .demo-value {
            background: white;
            padding: 6px 12px;
            border-radius: 6px;
            font-family: monospace;
            color: #666;
            border: 1px solid #e0e0e0;
            font-size: 13px;
        }
        
        .footer {
            text-align: center;
            margin-top: 30px;
            padding: 15px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            backdrop-filter: blur(10px);
        }
        
        .footer p {
            color: rgba(255, 255, 255, 0.8);
            font-size: 12px;
            margin-bottom: 5px;
        }
        
        .footer .version {
            color: rgba(255, 255, 255, 0.6);
            font-size: 10px;
        }
        
        .loading {
            display: inline-block;
            width: 16px;
            height: 16px;
            border: 2px solid #ffffff;
            border-radius: 50%;
            border-top-color: transparent;
            animation: spin 1s ease-in-out infinite;
            margin-right: 8px;
        }
        
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        
        .error-message {
            background: #fee;
            color: #c33;
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 15px;
            font-size: 12px;
            text-align: center;
            border: 1px solid #fcc;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <!-- Logo Section -->
        <div class="logo">
            <div class="logo-icon">🏢</div>
            <h1>Ruxchai Cold Storage</h1>
            <p>ระบบจัดการโครงสร้างองค์กร</p>
        </div>
        
        <!-- Login Card -->
        <div class="login-card">
            <!-- Welcome Message -->
            <div class="welcome">
                <h2>ยินดีต้อนรับ</h2>
                <p>เข้าสู่ระบบเพื่อเริ่มต้นการทำงาน</p>
            </div>
            
            <!-- Error Message -->
            <% if (typeof error !== 'undefined' && error) { %>
            <div class="error-message">
                <%= error %>
            </div>
            <% } %>
            
            <!-- Login Form -->
            <form id="loginForm" action="/login" method="POST">
                <% if (csrfToken) { %>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <% } %>
                <!-- Username -->
                <div class="form-group">
                    <div class="input-icon">👤</div>
                    <input 
                        type="text" 
                        name="username" 
                        id="username"
                        class="form-input" 
                        placeholder="ชื่อผู้ใช้งาน"
                        required
                        autocomplete="username"
                    >
                </div>
                
                <!-- Password -->
                <div class="form-group">
                    <div class="input-icon">🔒</div>
                    <input 
                        type="password" 
                        name="password" 
                        id="password"
                        class="form-input" 
                        placeholder="รหัสผ่าน"
                        required
                        autocomplete="current-password"
                    >
                </div>
                
                <!-- Remember Me -->
                <div class="checkbox-group">
                    <input type="checkbox" name="remember" id="remember">
                    <label for="remember">จดจำการเข้าสู่ระบบ</label>
                </div>
                
                <!-- Login Button -->
                <button type="submit" class="login-btn" id="loginBtn">
                    เข้าสู่ระบบ
                </button>
            </form>
            
            <!-- Demo Credentials -->
            <div class="demo-section">
                <div class="demo-title">ข้อมูลทดสอบ</div>
                <div class="demo-creds">
                    <div class="demo-item">
                        <span class="demo-label">ผู้ดูแลระบบ:</span>
                        <span class="demo-value">admin / admin123</span>
                    </div>
                    <div class="demo-item">
                        <span class="demo-label">ผู้ใช้งานทั่วไป:</span>
                        <span class="demo-value">user / user123</span>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <p>© 2025 Ruxchai Cold Storage Management System</p>
            <div class="version">v2.0 • Secure Access • 24/7 Support</div>
        </div>
    </div>

    <script>
        console.log('Login page loaded');
        
        // Auto-focus username field
        document.addEventListener('DOMContentLoaded', function() {
            document.getElementById('username').focus();
        });
        
        // Form submission handler
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            console.log('Form submitted');
            
            const username = document.getElementById('username').value;
            const password = document.getElementById('password').value;
            const button = document.getElementById('loginBtn');
            
            console.log('Username:', username);
            console.log('Password length:', password.length);
            
            if (!username || !password) {
                alert('กรุณากรอกชื่อผู้ใช้และรหัสผ่าน');
                e.preventDefault();
                return;
            }
            
            // Show loading state
            button.innerHTML = '<span class="loading"></span>กำลังเข้าสู่ระบบ...';
            button.disabled = true;
            
            console.log('Form data ready to submit');
        });
        
        // Quick fill demo credentials
        document.addEventListener('keydown', function(e) {
            if (e.ctrlKey && e.key === '1') {
                e.preventDefault();
                document.getElementById('username').value = 'admin';
                document.getElementById('password').value = 'admin123';
                console.log('Admin credentials filled');
            } else if (e.ctrlKey && e.key === '2') {
                e.preventDefault();
                document.getElementById('username').value = 'user';
                document.getElementById('password').value = 'user123';
                console.log('User credentials filled');
            }
        });
        
        // Click demo credentials to fill
        document.querySelectorAll('.demo-value').forEach(function(element, index) {
            element.style.cursor = 'pointer';
            element.addEventListener('click', function() {
                if (index === 0) {
                    document.getElementById('username').value = 'admin';
                    document.getElementById('password').value = 'admin123';
                    console.log('Admin credentials filled via click');
                } else {
                    document.getElementById('username').value = 'user';
                    document.getElementById('password').value = 'user123';
                    console.log('User credentials filled via click');
                }
            });
        });
    </script>
</body>
</html>