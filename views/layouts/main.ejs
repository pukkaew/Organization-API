<!-- Path: /views/layouts/main.ejs -->
<%- include('../partials/header', { 
    title: typeof title !== 'undefined' ? title : 'Organization Management',
    includeChartJS: typeof includeChartJS !== 'undefined' ? includeChartJS : false
}) %>
<%- include('../partials/navigation', { 
    activeMenu: typeof activeMenu !== 'undefined' ? activeMenu : '',
    user: typeof user !== 'undefined' ? user : null,
    success: typeof success !== 'undefined' ? success : null,
    error: typeof error !== 'undefined' ? error : null,
    warning: typeof warning !== 'undefined' ? warning : null,
    info: typeof info !== 'undefined' ? info : null
}) %>

<!-- Main Content -->
<main class="flex-1 py-8">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <%- typeof body !== 'undefined' ? body : '' %>
    </div>
</main>

<%- include('../partials/footer', {
    pageScripts: typeof pageScripts !== 'undefined' ? pageScripts : null
}) %>